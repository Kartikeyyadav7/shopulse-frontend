const styles = (theme) => ({
	products: {
		width: 600,
		marginRight: theme.spacing(4),
		paddingRight: theme.spacing(4),
	},
	paper: {
		margin: theme.spacing(4),
		width: 500,
	},
});

export default styles;
